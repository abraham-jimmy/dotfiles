# OpenCode

OpenCode CLI/TUI personal preferences.

## Files

- `opencode.json`: main config.
- `tui.json`: terminal UI config.

## Highlights

- Theme: `catppuccin`.
- Message navigation keybinds for page and line scrolling.
- Exit keybind includes `<leader>q` in both config files.
